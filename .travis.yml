language: swift
osx_image: xcode9.3
podfile: MarkPredictor/Podfile
before_install:
 - gem update cocoapods 
 - pod install 
script:
  - xcodebuild clean test -scheme MarkPredictor -workspace MarkPredictor.xcworkspace -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 7,OS=11.2' -enableCodeCoverage YES | xcpretty
after_success:
  - bash <(curl -s https://codecov.io/bash)
